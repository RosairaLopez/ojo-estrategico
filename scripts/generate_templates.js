const fs = require('fs'); const path=require('path'); const OUT=path.join(__dirname,'..','data','templates.json'); if(!fs.existsSync(path.dirname(OUT))) fs.mkdirSync(path.dirname(OUT)); const programs=['Socios por la Vision Empresarial','Renueva tu Enfoque','Descubre Tu Mejor Version']; let templates=[]; let id=1; for(let p=0;p<3;p++){ for(let month=1;month<=6;month++){ for(let day=1;day<=30;day++){ templates.push({id:id++, program:programs[p], month, day, channel: ['WhatsApp','LinkedIn','Instagram'][(day+month)%3], title:`${programs[p]} - Mes ${month} - Dia ${day} - Plantilla ${id}`, text:`Contenido unico para ${programs[p]}, mes ${month}, dia ${day}.`}); } } } fs.writeFileSync(OUT, JSON.stringify(templates,null,2)); console.log('Generated', templates.length);